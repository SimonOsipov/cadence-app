---
type: spec
project: cadence
status: approved
priority: p1
created: 2026-08-15
todoist_parent: "6hH8gf79GmFmvcMq"
components: [protocol, dosing, inventory, journal, data-layer, api, kmp-app]
proposal: "[[20-Projects/cadence/architecture/proposals/schedule-on-the-server|architecture/proposals/schedule-on-the-server]]"
---
<!-- SNAPSHOT (read-only copy). Master: 20-Projects/cadence/specs/protocol-dosing-and-schedule.md in vault prll-vault. Edit the vault note, then re-export — never edit here. -->

# Протокол, дозирование и расписание: серверная сторона

## Описание

Первый блок, дающий клиническому ядру сервер. Сегодня в API ровно один эндпоинт
(`GET /v1/me`), а расписание считает телефон — тот самый генератор, который дашборду
врача недоступен. Блок заводит схему шести таблиц, поднимает генерацию вхождений на
Go, даёт врачу CRUD протокола, а пациенту — запись дозы и два агрегата, на которых
стоят «Сегодня» и расписание. Kotlin-версия генератора остаётся жить до тех пор, пока
экраны не начнут читать API: её удаление — последний шаг блока подключения, а не этого.

Экраны при этом **не подключаются**: они уже портированы и живут на моках, а перевод
их на живой API — отдельный блок (фаза 4). Здесь строится то, к чему они будут
подключаться.

## User Story

**As a** clinic doctor
**I want** the schedule my patient sees to be the schedule I see
**So that** a conversation about a missed dose is about the dose, not about whose
screen is right

## Acceptance Criteria

- [ ] Генерация вхождений есть в Go и проходит по перенесённым фикстурам; Kotlin-версия
      **остаётся** до блока подключения экранов — её зовут девятнадцать файлов, и
      удаление здесь обрушило бы сборку
- [ ] Все четыре KMP-набора перенесены построчно: те же фикстуры, те же имена, каждая
      помеченная «measured» регрессия — включая событие без слота времени, два
      препарата в одном слоте, `N_PER_WEEK` в общей ветке с `WEEKLY` и несортированные
      фазы
- [ ] Шесть таблиц заведены цепочкой миграций с `ENABLE` **и** `FORCE ROW LEVEL SECURITY`;
      набор тестов политик расширен на каждую — это правило приёмки проекта, не пожелание
- [ ] Пациент читает только своё, назначенный врач — только своих, переназначение
      меняет видимость немедленно; проверено крест-накрест на двух врачах и двух пациентах
- [ ] Фазы одной позиции не перекрываются, и это правило **базы** (`EXCLUDE USING gist`),
      а не только Go; разрывы между фазами законны — их даёт `BROKEN_PHASES`
- [ ] Врач заводит протокол с позициями трёх видов и фазами титрации только своим
      пациентам, всё аудируется
- [ ] Препарат, которого нет в справочнике, заводится записью: имя нормализуется,
      повтор по регистру возвращает существующий `compoundId`, а не создаёт второй
- [ ] `POST /v1/me/dose-events` идемпотентен по ключу клиента: повтор возвращает тот же
      результат и не создаёт вторую строку — проверено, а не заявлено
- [ ] Одно действие — два факта: запись дозы и запись дневника в одной транзакции;
      источник дневниковой записи ставится при создании и не переписывается
- [ ] Остаток флакона нигде не хранится: он считается как `total_doses` минус число
      событий на этот флакон
- [ ] `GET /v1/me/today` и `GET /v1/me/schedule` отдают ровно те формы, которые ждут
      `TodaySummary` и `ScheduleDay`; поля несуществующих контекстов отдают `null`
      явно, а не ноль
- [ ] Подсказка следующего места инъекции считается на сервере из истории
- [ ] Фото за подписанной ссылкой: публичных URL нет, право проверяется по RLS строки
      прежде, чем ссылка выдаётся
- [ ] `make gate` и `make test-integration` зелёные; `openapi.json` перегенерирован и
      закоммичен

## Scope / Non-scope

**В объёме.** Схема и политики шести таблиц · генерация вхождений на Go · CRUD
протокола и пополнение справочника · запись дозы с идемпотентностью · агрегаты
`/me/today` и `/me/schedule` · подсказка ротации · хранилище и подписанные ссылки ·
расширение `cmd/seed` протоколом.

**Вне объёма, названо явно.** Подключение мобильных экранов к API — фаза 4, вместе с
кэшем чтений и очередью повтора. Редактор протокола в дашборде — **отдельная спека**:
он зависит от «Скелета дашборда» и «Первого живого чтения», которых ещё нет, и держать
его здесь значило бы иметь спеку, которую нельзя доделать. CRUD флаконов — M4: здесь
флакон только **разрешается** для записи дозы, не заводится. Колонки протокола в
реестре врача — M6, как записано в «Первом живом чтении».

## Что уже реализовано (DONE)

- **Генератор вхождений в KMP, 142 строки** — `Occurrence.kt`. Недели считаются от
  `startDate` (`cycleWeek`, `:32-40`); отменённый протокол не даёт ничего, завершённый
  сохраняет историю (`:63`); `fallsOn` (`:111-115`) и `scheduledDaysPerWeek`
  (`:104-108`) делят ветку между `WEEKLY` и `N_PER_WEEK` намеренно; `phaseDose`
  (`:90-98`) берёт **первую** покрывающую фазу без сортировки; `statusOf` (`:122-142`)
  сопоставляет событие со слотом по трём полям сразу.
- **Соседи:** `ProtocolRow.kt` (полоса недели, `leastComplete` `:67-72`), `DoseBands.kt`
  (полосы предписания, игнорируют события), `TitrationStep.kt` (точки смены дозы).
- **Четыре набора тестов** — `OccurrenceTest.kt`, `WeekProtocolTest.kt`,
  `DoseBandsTest.kt`, `TitrationTest.kt`. Это поведенческая спецификация, а не тесты.
- **Доменные типы:** `Protocol`, `ProtocolItem`, `ProtocolPhase`, `Compound`,
  `DoseEvent`, `DoseDraft`, `Vial` (`Protocol.kt`, `Dosing.kt`, `DoseDraft.kt`, `Vial.kt`).
  У `Vial` **нет** полей `status` и `remaining` — оба выводятся (`InventoryMath.kt`).
- **Три интерфейса репозиториев**, которые API обязан удовлетворить: `TodayRepository`,
  `ScheduleRepository`, `DoseLogRepository`. Объявлены в `shared`, а не за моком,
  именно чтобы настоящая реализация была подменой, а не переделкой.
- **Образец контекста на Go** — `api/internal/identity/`: `routes.go` (по
  `huma.Register` на операцию), `handler.go` (структуры с тегами `doc:`), и **один файл
  на агрегат** (`patients.go`), где смешаны проверка, транзакция и SQL. Разделения
  `service.go`/`repository.go` в проекте нет.
- **Швы:** `database.WithService` (сервисный путь, актёр из принципала, аудит),
  `database.WithCaller` (путь запроса, имперсонация роли, RLS применяется).

**Чего нет.** `api/internal/{protocol,dosing,inventory}/routes.go` — пустые заглушки
`func Register(huma.API) {}`. Ни одной из шести таблиц в `api/migrations/` (там семь
пар, все про идентичность). Ни `cmd/seed`, ни какого-либо кода объектного хранилища —
грепом по всему `api/` не находится ничего.

## Технические детали

**Порядок миграций** идёт по направлению зависимостей: протокол ни от кого не зависит,
аптечка и дневник зависят от протокола, дозирование — от всех трёх. Пары «таблицы» и
«политики» раздельны, как у идентичности: таблица с `FORCE RLS` и без политик — это
безопасное промежуточное состояние, а не недоделка.

**Перекрытие фаз запрещает база.** `EXCLUDE USING gist (protocol_item_id WITH =,
int4range(from_week, to_week, '[]') WITH &&)`. Равенство по `uuid` внутри `gist` требует
**`btree_gist`**: встроенный GiST покрывает диапазоны и геометрию, но не обычные скаляры.
Расширение в цепочке не включено ни разу за семь миграций, поэтому шаг 2 включает его
сам — иначе миграция падает на чистой базе, то есть на любой новой машине и в CI. Разрывы при этом законны — `BROKEN_PHASES`
в `DoseBandsTest` доказывает, что пауза между фазами это осознанный вымыв, а не ошибка.
Go проверяет то же самое до вставки, чтобы врач получил 422 с указанием строки, а не
сырой `23P01`; ограничение базы остаётся сторожем гонки, которую Go не видит.

**Дни недели — ISO.** `days_of_week smallint[]`, где 1 = понедельник, 7 = воскресенье,
как `kotlinx.datetime.isoDayNumber`. В Go `time.Weekday` считает с воскресенья от нуля,
поэтому нужен один маленький проверенный помощник, а не «очевидное» приведение.

**Идемпотентность записи дозы** — `UNIQUE (patient_id, client_request_id)`. Повтор
находит строку и возвращает уже посчитанный ответ, не перепроверяя и не переписывая.
Отдельно `UNIQUE (protocol_item_id, scheduled_for_date, scheduled_for_time)` при
непустом времени — сторож гонки, когда два разных ключа метят в один слот.

**Остатка нет как поля.** `remaining = total_doses − count(dose_events WHERE vial_id = …)`.
Записать дозу значит поставить `vial_id` на новой строке; никакого уменьшения счётчика
не происходит, потому что счётчика нет — нечему разъезжаться.

**Путь записи и путь чтения разделены по швам.** Протокол пишет врач — это
кросс-акторная запись, значит `WithService` и строка аудита. Дозу, дневник и флакон
пишет пациент про себя — это `WithCaller`, RLS и без аудита: у `cadence_patient` нет
и не будет права писать в `audit_log`.

**Ответ записи дозы — не ошибка HTTP.** `written | incomplete | not_scheduled_today |
already_logged` приходят телом с кодом 200: клиент по ним уже ветвится, это ожидаемые
исходы, а не сбои. `problem+json` остаётся для настоящих ошибок.

**Хранилище — не Postgres, и RLS там нет.** План партнёра (PRO-03) говорит про
`storage.objects` с политиками RLS: это форма Supabase Storage, где метаданные объектов
лежали в Postgres. На Timeweb такого нет — ADR-008 этого следствия не проследил. Право
проверяется на эндпоинте: строка `vials`/`dose_events` должна быть видима вызывающему
под `WithCaller`, и только тогда выдаётся короткая подписанная ссылка. Ключ несёт
`{patientId}` в пути, поэтому утёкший ключ хотя бы говорит, чей он.

## Архитектурное решение

**Первое: генератор один и живёт на сервере** — но становится он единственным не здесь.
Обосновано в предложении; здесь важно следствие для порядка работ. Kotlin-версию зовут
девятнадцать файлов, включая моки и оба экрана, поэтому удалить её раньше, чем экраны
начнут читать API, невозможно физически: сборка встанет, а приложение останется без
данных. Удаление — последний шаг блока подключения, ровно как записано в предложении.

Отдельный корпус фикстур в JSON при этом не заводится: четыре файла тестов — **981
строка** (посчитано, а не прикинуто: 284 + 209 + 409 + 79), и одноразовый экспортёр,
которому нужна сериализация со стороны Kotlin и читатель со стороны Go, обошёлся бы
дороже построчного переноса, который к тому же читается на ревью. То,
что обе реализации какое-то время живут рядом, здесь работает на нас — соответствие
тестов проверяется сличением, а не памятью автора.

**Второе: интерфейсы репозиториев KMP — это контракт эндпоинтов.** `TodaySummary`,
`ScheduleDay`, `Occurrence`, `DoseLogResult` уже описывают форму ответа. Эндпоинты
проектируются под них, а не наоборот, иначе фаза 4 будет не подключением, а переделкой
экранов — ровно то, ради чего нулевой блок держал моки за интерфейсами.

**Третье: протокол не импортирует дозирование.** `OccurrencesFor` принимает узкий
`LoggedSlot{itemId, date, time}`, а не `dosing.DoseEvent`. Направление импорта
одностороннее: `dosing` знает про `protocol`, обратно — нет.

## Дельты компонентов

### protocol.md
- MODIFIED: инвариант 1 — правило «нет материализованной таблицы расписания» остаётся
  дословно, к нему добавляется «генератор один на продукт и живёт на сервере; клиент
  вхождения не вычисляет»
- ADDED: к Data — `compounds` пополняется не только миграцией; редактор принимает
  препарат не из справочника и заводит запись, имя нормализуется, дубли по регистру
  схлопываются
- ADDED: инвариант — фазы одной позиции не перекрываются, и это ограничение базы;
  разрывы между фазами законны
- ADDED: инвариант — у пациента **один активный протокол**; закреплено частичным
  уникальным индексом. Снятие ограничения потребует миграции, превращения
  `ProtocolPlan` в список и решения о том, что такое «доза на сегодня» при двух курсах
- ADDED: к Contracts — `GET /v1/me/today` и `GET /v1/me/schedule` принадлежат этому
  контексту: расписание — его предмет

### dosing.md
- ADDED: инвариант — ответ записи дозы это закрытое множество исходов в теле ответа,
  а не коды ошибок HTTP
- MODIFIED: инвариант 4 — «уменьшает остаток по построению» уточняется: остаток не
  уменьшается, а выводится; запись дозы лишь проставляет `vial_id`

### inventory.md
- ADDED: инвариант — флакон для записи дозы **разрешается**, а не выбирается: при двух
  открытых флаконах одного препарата сервер оставляет `vial_id` пустым, потому что
  угадывать нечем до появления выбора в M4

### api.md
- ADDED: инвариант — кросс-акторная запись идёт через `WithService` и оставляет строку
  аудита; запись пациента про себя идёт через `WithCaller` и аудита не оставляет, потому
  что у роли пациента нет права писать в журнал

### data-layer.md
- ADDED: к Storage — объектное хранилище не имеет RLS; право на файл проверяется по
  видимости строки-владельца под `WithCaller`, и только после этого выдаётся короткая
  подписанная ссылка

## Decomposition

### step-1: Генерация вхождений появляется на Go
Порт `Occurrence.kt`, `ProtocolRow.kt`, `DoseBands.kt`, `TitrationStep.kt` в
`api/internal/protocol/` чистыми функциями без базы, плюс построчный перенос четырёх
наборов тестов: те же фикстуры, те же имена, каждый комментарий «measured» дословно.
`OccurrencesFor` принимает `LoggedSlot`, а не событие дозы: направление импорта
одностороннее.

**Kotlin-генератор в этом шаге не удаляется.** Его зовут девятнадцать файлов — моки,
оба экрана, проводка оболочки и восемь наборов тестов; пока приложение живёт на моках,
удаление обрушит сборку и оставит его без данных вовсе. Удаление принадлежит блоку
«телефон перестаёт притворяться», последним шагом после того, как экраны начнут читать
API. Это и делает ревью сличением: обе реализации присутствуют рядом, и построчное
соответствие тестов проверяется глазами, а не памятью.
todoist: "6hH8gjMXm4r6JW2q"

> [!deviation] 2026-08-21 — шаг 1
> Шаг требует построчного переноса. Пять мест, где он не построчный, и почему.
>
> **1. `TrendRange` разрезан по двум поверхностям.** На сервер уехал `Range`
> (`From`, `Through`, `Contains`, `Days`); `fractionOf` и `middleOf` остались на
> клиенте — это раскладка холста, у сервера её нет. Это межповерхностное решение,
> а не пропуск: «Архитектурное решение», пункт 2, делает типы KMP контрактом
> эндпоинтов фазы 4, и `Range` в контракт войдёт, а доли холста — нет.
>
> **2. Инвариант конструктора `TrendRange` не поехал.** В `TrendWindow.kt:43-47`
> стоял `require(from <= through)`; у `Range` его нет.
>
> И перевёрнутое окно **не безвредно** — так было написано здесь сначала, и это
> неправда: `Contains` отвечает «нет» на всё, но `Days()` возвращает
> **отрицательное** число (замерено: −8 на окне, перевёрнутом на девять дней).
> Комментарий в `calendar.go` это уже говорит; запись отставала на два раунда.
>
> **Требование к шагу 9,** и оно про это: агрегаты строят окно из параметра
> `month` и **не должны** ни делить на `Days()`, ни итерировать по нему до того,
> как вернётся проверяющий конструктор.
>
> **3. `LocalDate` и `LocalTime` стали `Date{Year,Month,Day}` и `Slot{Hour,Minute}`.**
> В Go нет календарного дня без таймзоны, а весь матчер стоит на `==`, которое у
> `time.Time` ненадёжно: оно включает `*Location` и монотонное показание.
>
> **4. `MockSeed` и `TrendWindow.CYCLE.rangeOn` воспроизведены руками.** Мок живёт
> в `commonMain` и из Go недостижим. Следствие названо в самом файле: это второй
> взгляд на ту же арифметику, а не независимый свидетель, и две копии теперь могут
> разойтись молча.
>
> **5. Средняя проверка `aBandIsWhatWasPrescribedAndNotWhatWasTaken` не перенесена.**
> Kotlin сравнивает **множества**: `MockSeed.history` у семаглутида даёт ровно одно
> различное значение дозы — 0,25 мг, множество из одного элемента. Событий при этом
> три (10, 17 и 24 мая: `buildHistory` идёт по дням до `seededThrough`, а препарат
> воскресный), и все три лежат в первой полосе — в этом и смысл. `LoggedSlot` дозы
> не несёт по замыслу: направление импорта одностороннее. **Переезжает в блок
> `dosing`, шаг 8, и утверждать там надо множество значений, а не число событий.**
>
> **6. Закрытые множества стали именованными строками.** `enum class` Kotlin делает
> посторонее значение непредставимым; `type ProtocolStatus string` — нет. Замерено:
> `Status: "CANCELLED"` (ровно то, что даст сериализованный Kotlin-enum) читается
> всеми **шестью** сравнениями статуса как «не отменён», и отменённый курс выдаёт
> полное расписание с дозами. То же у `Cadence`: неизвестное значение молча выпадает
> из расписания и обнуляет `DosesPerWeek`. **`CHECK` — на шаге 2, `Parse*` в форме
> `(T, bool)` — на шаге 6.** Разные шаги намеренно: шаг 2 это миграции и политики, а
> строка становится значением Go впервые в CRUD шага 6 (и в сиде шага 11) — там и
> единственный шов, где постороннее значение может прийти. Двух проверок не заводим.
>
> **7. `Date` не канонична по построению.** `LocalDate` Kotlin непредставим неверным;
> `Date{2026, February, 30}` — представим, и это хуже, чем просто неверное значение:
> `==` говорит «разные дни», а `Before`/`After` — «один и тот же», потому что первое
> сравнивает поля, а второе идёт через `time`. Запись с такой датой не закроет
> вхождение того дня, который она обозначает. **Возвращается на шаге 6** тем же
> швом, что и `Parse*`: `Date.Valid()` — одна строка, `fromTime(d.time()) == d`.
>
> **8. Полосы и метки клипаются и по началу курса — Kotlin клипает только конец.**
> Единственное место, где порт **меняет поведение**, а не переносит. `WeekStart(0)` — это
> неделя до старта протокола, а §03 оставляет `from_week` таким же непривязанным, как
> `to_week`. Замерено на фазе с `from_week = 0`: `doseBands` рисует полосу с 3 мая при
> старте 10 мая, а `phaseDose` на 3 мая отвечает `null` — два читателя одних и тех же
> фаз расходятся о дне, который график рисует. Kotlin ведёт себя так же; дневной обход
> `theBandCoveringADayCarriesTheDoseThatDayWasPrescribed` идёт по смещениям 0..83 и ниже
> нуля заглянуть не может. Асимметрия в оригинале выглядит недосмотром, а не решением:
> дальний конец клипается, ближний нет. Закрыто `MaxDate(WeekStart(from), StartDate)` и
> симметричной проверкой в метках, тест — `aPhaseOpeningBeforeTheCourseIsCutAtItsFirstDay`.
> **При удалении Kotlin-генератора это расхождение снимается само; до тех пор две
> реализации отвечают по-разному на протокол с `from_week = 0`.**
>
> ↺ **Устарело с шага 2.** Такая строка в базу больше не попадает:
> `CHECK (from_week >= 1)` в `000013`. Клип остаётся свойством порта — он держит
> вход, который тип допускает, а схема нет, — но живого расхождения поведения
> между Kotlin и Go на реальных данных **нет**, и шагу 12 записывать в реестр
> расхождений нечего. Фикстура `aPhaseOpeningBeforeTheCourseIsCutAtItsFirstDay`
> описывает строку, которую схема отвергает; это осознанно.
>
> Сверх переноса добавлено десять тестов: шесть на календарь, два на направление
> импорта и чистоту, стабильность порядка фаз и метка за концом курса. Плюс шесть
> по итогам ревью — они закрывают оси, которых не мерил ни один из двух наборов.
> Один перенесённый тест при этом **переписан, а не дополнен**:
> `theStripCarriesWhichItemsCanBeLogged` строит свой план с четвёртым пунктом —
> инъекцией, которую не логируют, — потому что в исходной фикстуре `loggable`
> выводился из `kind` и колонка §03 ничем не проверялась. Утверждение строго сильнее
> исходного.

### step-2: Схема протокола и её политики
`compounds`, `protocols`, `protocol_items`, `protocol_phases` парой миграций.
`CREATE EXTENSION IF NOT EXISTS btree_gist` и `EXCLUDE USING gist` на перекрытие фаз. Политики шести форм: пациент читает своё, назначенный
врач — своих через `care_team_assignments`, админ всё, сервисный путь узко. Набор
тестов политик расширяется на четыре таблицы крест-накрест.
todoist: "6hH8gjQ93jr2MmhH"

> [!deviation] 2026-08-21 — шаг 2
> **Появилась седьмая форма политики.** `data-layer.md`, инвариант 2, перечисляет
> шесть форм и говорит «и никаких других»; шаг тоже сформулирован как «политики
> шести форм». `compounds_read ... FOR SELECT TO cadence_patient, cadence_doctor
> USING (true)` — седьмая: справочник не привязан к пациенту, и фильтровать в
> строке нечего. Оставить таблицу без политики нельзя — под `FORCE` это отказ
> всем.
>
> Инвариант 2 при финализации **переписывается, а не дополняется**: иначе в ноте
> окажется «шесть и никаких других» рядом с седьмой. Формулировка седьмой:
> *справочник, не привязанный к арендатору, читаемый обеими запросными ролями —
> и читаемый **по колонкам**, а не целиком.*
>
> Колоночность — часть формы, а не деталь: `code` говорит, завела ли препарат
> миграция или клиника, `created_at` говорит когда, и вместе они сообщают любому
> пациенту, что кому-то только что назначили новое, с точностью до секунды.
> Замерено на живой цепочке; закрыто грантом на пять колонок.
>
> **Появилась и восьмая форма, и её тоже не было в шести.** Политики
> `protocol_phases` **не называют субъекта вообще**: оба тела запросных ролей
> идентичны и различаются только `TO`, а «чьё» отвечает подзапрос, исполняемый под
> политиками `protocol_items`. Реестр фиксирует это одной константой на две роли.
>
> Две постоянные издержки, и они принадлежат записи, а не одному файлу: гарантия
> **целиком транзитивна** — политика `protocol_items`, переставшая зависеть от
> субъекта, откроет и эту таблицу без единой правки в её файле; и добавление роли
> в `TO` любого из двух тел — это вся правка целиком.
>
> **Решение для шагов 4 и 5.** `journal_entries` и `dose_events` — тоже потомки
> `protocols`/`protocol_items`, и перед ними встанет тот же выбор. Записано здесь,
> чтобы они не решали заново: опираться на родителя допустимо ровно тогда, когда у
> таблицы **нет собственной колонки владельца**. У обеих она есть (`patient_id`),
> значит шаги 4 и 5 выписывают предикат целиком.
>
> При финализации инвариант 2 `data-layer.md` переписывается под **восемь** форм,
> а не под семь.
>
> **Гранты на владеющие колонки сужены по той же причине.** Сервисный путь не
> несёт предиката строки, поэтому от переноса строки между пациентами защищает
> только грант — политика этого сказать не может: `WITH CHECK` видит новую
> строку, а `OLD` у RLS нет. Замерено: при табличном `UPDATE` один statement
> переносил позицию чужого пациента на курс этого, и тот читал её собственной
> политикой. `UPDATE` на `protocols.patient_id`, `protocol_items.protocol_id` и
> `protocol_phases.protocol_item_id` снят; `INSERT` остаётся табличным.
>
> **Реестр колонок стал ключеваться по глаголу.** Он читал только `UPDATE`, а
> колоночный `SELECT` на `compounds` был бы привилегией, которую никто не
> сверяет, — ровно тот отказ, ради которого существует инвариант 6.
>
> **Что грант закрыть не может, и это принадлежит шагу 6.** Перенос строки
> **на месте** закрыт. Копирование с удалением — нет и не может быть: у `DELETE`
> колоночной формы не существует, а строкового предиката у сервисного пути нет
> намеренно. Замерено: `INSERT … SELECT` чужой позиции на свой курс плюс `DELETE`
> оригинала даёт тот же результат. Отказ этому — в Go, шаг 6.
>
> **Шаг 6 дублирует в Go семь правил формы строки, а не одно.** Спека говорит ему
> «проверка покрытия фаз»; на деле база отвергает семью ограничениями, и каждое
> шаг 6 обязан проверить до вставки, чтобы врач получил 422 с указанием строки, а
> не сырой `23514`: перекрытие фаз (`EXCLUDE`), `from_week >= 1`,
> `protocol_phases_runs_forwards`, `protocol_items_cadence_matches_days`,
> `protocol_items_has_a_slot`, `protocol_items_slots_are_a_named_flat_list`,
> `protocol_items_days_are_a_flat_list`.
>
> **Три вещи, замеренные и оставленные шагам, которые их встретят.**
> 1. `compounds` не выдан `UPDATE` сервисному пути: столкновение имён не должно
>    переписывать справочную строку, на которую уже ссылаются чужие протоколы.
>    Следствие для шага 6 — идиома `INSERT … ON CONFLICT DO UPDATE … RETURNING`
>    там будет отвергнута, нужна `ON CONFLICT DO NOTHING` плюс `SELECT`.
> 2. `protocol_items.compound_id` остаётся обновляемым. Само число не уезжает:
>    §03 даёт `dose_events` собственные `dose_value` и `dose_unit`, так что
>    записанная доза снята на событие и переживает правку. Уезжает **атрибуция** —
>    то, к какому препарату отнесена история, а клинически это факт весомее числа.
>    Решать шагу **5**: колонка становится решаемой ровно тогда, когда появляется
>    `dose_events.protocol_item_id`, а это его миграция, не восьмого шага. Это
>    целостность истории внутри одного пациента, не изоляция.
> 3. `compounds.id` обязан оставаться случайным (сейчас uuid v4, замерено).
>    Упорядоченный по времени uuid вернул бы `created_at` в колонку, которую
>    пациент читает, — то есть отменил бы сужение канала выше.
>
> **Что оба реестра грантов не видят** (ограничение общее с блоком идентичности,
> не новое здесь): прямой грант роли **вне** `ChainRoles()`. Свод членств ловит
> чужака, включённого в cadence-роль, но не грант ему напрямую.
>
> Замерено, и это меняет вес находки: такой грант **неработоспособен**. 000001
> выдаёт `USAGE` на схему `app` только четырём целям олицетворения и отзывает его
> у `PUBLIC`, поэтому посторонняя роль с колоночным грантом до таблицы не дойдёт —
> нужен второй, столь же намеренный грант. Дыра невидима и мертва, а не невидима
> и жива. Чинится тем же приёмом, что и отсутствующая строка ACL у свидетеля
> отката — обходом `relacl` вместо опроса роль за ролью, — и обе это своя правка
> со своим радиусом, а не хвост этого шага.

### step-3: Схема аптечки и выводимые величины
`vials` парой миграций, без полей `status` и `remaining`. Порт `InventoryMath.kt`:
остаток, статус, подсказка докупки. HTTP пока нет — только функции, которыми
пользуются соседи.
todoist: "6hH8gjWpq3xGQfJH"

> [!deviation] 2026-08-21 — шаг 3
> **`remainingDoses` принимает флаконы, а не события дозы.** В Kotlin сигнатура
> `(Vial, List<DoseEvent>)`; в Go — `(Vial, []VialID)`, то есть «из какого флакона
> взята каждая записанная доза», и ничего больше. То же сужение и по той же
> причине, что `LoggedSlot` у `OccurrencesFor` на шаге 1: направление импорта
> одностороннее, `inventory` не узнаёт, что такое событие дозы. Обе половины
> закреплены тестом, включая **положительную**: пакет, переставший читать
> `protocol`, — это подсказка докупки, тихо потерявшая свою ставку.
>
> **Инвариант 3 ноты `inventory` не реализован, и это решение, а не пропуск.**
> Нота требует считать недели запаса от **текущей фазы титрации**, со справочной
> ставкой препарата как фолбэком; то же говорят канонические документы партнёра.
> Kotlin не делает ни того, ни другого — он берёт число слотов в неделю, и доза в
> арифметику не входит вовсе. Перенесён Kotlin.
>
> Причина в том, что модель **счётная**: `total_doses` — это число введений, а не
> количество вещества, остаток уменьшается на единицу независимо от дозы, и
> `concentration_label` намеренно «ярлык, а не число». Инвариант предполагает
> количественную модель, схема выбрала счётную, поэтому он недостижим **без
> миграции**: при переходе на следующую фазу `total_doses` не меняется.
> То есть шаг 3 не обошёл инвариант, а закрыл его схемой — и это уровень
> компонента, значит запись обязательна.
>
> **`ReorderHintFor` берёт `Cabinet`, а не срез флаконов.** Расхождение с Kotlin,
> и по той же причине, что два предыдущих: функция суммирует остатки и ищет
> запечатанный запас по всему, что ей дали, а на стороне врача одна политика
> отдаёт кабинеты нескольких пациентов одним срезом — и каждая строка в нём
> законна, поэтому база смешивание не поймает. `CabinetOf` фильтрует один раз,
> поле не экспортировано, и построить смешанный кабинет нечем. До этого
> `Vial.PatientID` не читал никто, а фикстура была одноарендной, то есть функция
> вела бы себя так же с удалённым полем.
>
> **Наблюдение, которое я не воспроизвёл, и оно записано именно поэтому.** Один
> прогон полного гейта уронил интеграционный набор; два последующих, включая
> `-count=1`, зелёные, текст падения не сохранён. Шаг измеримо изменил вход:
> подзадачи `TestTheReachOfEachRoleOverTheCabinet` строят каждая свою клинику
> (по замечанию ревью), и пакет `inventory` пошёл с пяти полных прогонов цепочки
> на восемь. Каждый — `CREATE DATABASE` плюс шестнадцать миграций; контейнеров
> кластер поднимают **шесть** пакетов (`identity`, `inventory`, `protocol`,
> `platform/auth/token`, `platform/database`, `platform/testsupport`), пакеты идут
> параллельно, `-race` включён. Пять было до этого шага — недосчитан ровно тот
> пакет, который шаг и добавил.
>
> Следующий замер — `go test -p 1`: если при последовательных пакетах не
> воспроизводится за десяток прогонов, это ресурс (старт контейнера, исчерпание
> соединений), а не общий объект. Роли цепочки — объекты **кластера**, но внутри
> пакета тесты последовательны, а между пакетами контейнеры разные, так что
> общего объекта не видно — это рассуждение, не измерение. Непроверенный отказ в
> наборе, который и есть доказательство модели доступа, читается как пройденный:
> см. [[a-timed-out-mutation-run-ships-the-mutation]].
>
> **Инвариант 2 той же ноты расходится тоже, и мягче.** Она говорит «less than
> four weeks of supply», код и §03 говорят «≤ 4 недели» — порог ровно в четыре
> недели даёт подсказку, и это закреплено тестом. Расхождение на одну неделю, но
> при финализации инвариант 2 правится вместе с третьим, иначе M4 унаследует
> «< 4», переписывая его под счётную модель.
>
> **Требование к M4** (там живёт CRUD флаконов): либо завести ставку в
> `compounds` и передавать дозу фазы в `ReorderHintFor` — тогда нужна миграция
> поверх закрытой пары шага 2, — либо переписать инвариант 3 под счётную модель,
> и это уже предложение, потому что инвариант компонента.
>
> **`[!decision]` от 15.08 про докупку говорит в настоящем времени** — «аптечка
> считает по текущей фазе титрации, что точнее» — о коде, который так не делает,
> и опирается на «ставку из миграции», которой в `app.compounds` нет. При
> финализации формулировка переводится в будущее.
>
> **Флакон пишет сам пациент — запросный шов, RLS, без аудита** (у
> `cadence_patient` нет права писать в `audit_log`), в отличие от протокола,
> который врач пишет про пациента сервисным швом. Его предикат здесь поэтому не
> фильтр чтения, а ещё и правило записи: он стоит и в `USING`, и в `WITH CHECK`.
>
> **Форма при этом не новая, и это важно.** Блок идентичности уже пишет так три
> таблицы, и константа `ownRowWrite` в реестре предикатов — это тот же субъект в
> обеих половинах, применённый к `profiles`, `patient_profiles` и
> `user_preferences`. Новое здесь у́же: это первый `INSERT`, который вообще есть у
> роли `cadence_patient`, и первая патиентописуемая таблица, где владелец —
> обычная колонка, а не первичный ключ строки.
>
> **Прецедент велит сужать по колонкам, и `vials` ему следует.** Две из трёх
> названных таблиц идентичности сужены, и это не про владение — владение здесь
> держат политики, поэтому `patient_id` намеренно остаётся в обоих списках.
> Сужены **две** колонки, и в обоих глаголах одинаково: `id` (на него будут
> ссылаться события доз шага 8, а выбранный вызывающим ключ — это ещё и способ
> спросить, существует ли угаданный) и `created_at` (происхождение строки, а не
> поле формы). Списки `INSERT` и `UPDATE` держатся согласованными сознательно:
> `disposed_at` сначала стоял только в `UPDATE`, и это был тот же тупик, что с
> ключом фото — флакон, выброшенный до того, как клиника его записала, M4 не смог
> бы завести. Первая версия шага
> выдала табличный грант, обосновав это ложным утверждением, будто форма новая;
> ревью замерило дыру и вернуло прецедент.
>
> **`label_photo_path` — третий случай, и он решён иначе, чем два первых. Это
> единственное, что стоит между пациентом и подписанной ссылкой на чужой объект,
> поэтому здесь оно записано целиком.**
>
> Колонка — ключ в объектном хранилище, **у которого RLS нет**. Эндпоинт шага 10
> проверяет, что **строка** видима вызывающему, и подписывает то, что говорит
> **ключ**. Сужать колонку пробовали первым — и это сделало её незаписываемой на
> всех продуктовых путях: флаконы пациент пишет запросным швом, у сервисного нет
> `UPDATE`, а фото прикрепляют после создания. M4 упёрся бы в `42501` и починил
> самым дешёвым способом — перенёс бы запись флакона на сервисный шов, молча
> отменив решение о шве.
>
> Поэтому колонка выдана пациенту, а маршрут закрыт ограничением схемы
> `vials_photo_key_is_under_its_own_prefix`. Оно **сильнее гранта**: связывает
> владельца, админа, сервисный путь и любую роль, которую заведёт будущая
> миграция — замерено, суперпользователь тоже отвергается. Грант так не умеет.
>
> Форма ограничения — регулярное выражение, а не префикс, и разница замерена:
> `LIKE '<uuid>/%'` пинит **начало строки**, а не адрес объекта, поэтому
> `<свой>/../<чужой>/label.jpg` его проходил, а `path.Join` и `path.Clean` оба
> разрешают такой ключ в объект другого пациента. Выражение допускает от одного до
> четырёх сегментов, каждый начинается с буквы или цифры — значит `..` не сегмент,
> `//` не сегмент, нет ни хвостового слэша, ни перевода строки, ни голого
> префикса. Длина живёт в самом выражении, а не рядом с ним.
>
> **Два следствия для шага 10.** Раскладка ключа зафиксирована здесь —
> `{patient_id}/…`, без сегмента бакета, — так что переход на ключ,
> квалифицированный бакетом, это миграция. И защита **позиционная, а не
> нормализующая**: она отвергает поднимающийся ключ, но не переписывает его,
> поэтому подписывающий не должен нормализовать путь перед подписью.
>
> **Замерено про две половины предиката, потому что читается неверно.** Кражу
> строки через `UPDATE … WHERE id = …` отвергают обе — `SELECT`-политики
> подключаются к `UPDATE`, чей `WHERE` читает колонку. Но `UPDATE` **без `WHERE`**
> не читает ничего, `SELECT`-политики к нему не подключаются, и `USING` у
> `vials_own_update` остаётся единственным сторожем — а сметает такой statement всю
> таблицу разом. Проверка кражи написана в трёх формах именно поэтому.
>
> **`DELETE` нет ни у кого, кроме админа.** Флакон утилизируют, проставляя
> `disposed_at`; события доз, взятых из него, должны продолжать на него ссылаться.
> «Убрать в резерв» и «утилизировать» — оба `UPDATE`.
>
> **Врач читает аптечку своих пациентов** через `care_team_assignments` — как и
> всякую другую таблицу, привязанную к пациенту. Не пишет.
>
> **Пишет пациент — и сервисный путь тоже, `INSERT`.** Здесь запись сначала
> говорила «пишет только пациент», и это было неверно. `vials_service_insert` есть,
> грант **табличный**, предиката строки у него нет и по колонкам он не сужен — та
> самая конструкция, из-за которой протокольным таблицам понадобились колоночные
> гранты. Нужен сиду и разрешению флакона при записи дозы (шаг 8). `UPDATE`
> сервисному пути не выдан намеренно, и это закреплено подтестом.
>
> **Ручки HTTP не заводятся** (их нет в шаге), а CRUD флаконов принадлежит M4;
> здесь появляются таблица, политики и математика, которой пользуются соседи.
>
> **Две дыры, замеренные мутацией, есть и в Kotlin-наборе.** Порог «≤4 недели»
> сдвигался на пятую неделю без единого падения — ни одна фикстура не давала запас
> ровно в четыре или пять недель.
>
> Второе — страж нулевой недельной ставки, и здесь важна поправка: **первый замер
> был сделан на одной архитектуре и оказался неверен для той, где код работает.**
> Конверсия `±Inf` и `NaN` в `int` спецификацией Go не определена, и цели
> расходятся (замерено 21.08):
>
> | | `int(4/0)` | `int(0/0)` |
> |---|---|---|
> | arm64 (ноутбук) | максимальное int64 | 0 |
> | amd64 (CI, Timeweb) | минимальное int64 | минимальное int64 |
>
> На arm64 непустой флакон случайно давал «нет подсказки», потому что максимум
> больше порога; на amd64 обе конверсии дают минимум, который порога не превышает,
> — то есть **без стража прод выдал бы подсказку и на полном флаконе**, с числом
> недель в минус девять квинтиллионов. Страж стоит до деления именно поэтому.
> Обе границы закрыты тестами, и тест взят на пустом флаконе — это случай, который
> падает на обеих архитектурах. См. [[the-gate-measures-one-platform]].

### step-4: Схема дневника и запись из дозы
`journal_entries` парой миграций с `PRIMARY KEY (patient_id, entry_date)`. Функция
слияния: названные поля перекрывают, неназванные сохраняются, метки копятся без
дублей, **источник при конфликте не трогается** — правило уже реализовано и проверено
на стороне KMP, здесь оно повторяется на сервере.
todoist: "6hH8gjVXCR5XpJwH"

> [!deviation] 2026-08-22 — шаг 4
> **Закрытое множество тегов живёт в `journal`, а не в `dosing`.** В KMP это
> `typealias JournalTag = SideEffect` в файле дозирования: два множества, которые
> не должны расходиться, — это одно множество, и Kotlin может себе позволить
> положить его где угодно, потому что защищать направление импорта ему не от чего.
> В Go есть от чего: одно действие пациента пишет событие дозы **и** запись
> дневника, значит `dosing` читает `journal`, и множество обязано лежать на том
> конце, куда запись приходит второй. **Шаг 5** заводит `dose_events.side_effects`
> и ссылается на то же множество — свою копию не создаёт.
>
> **Множество теперь записано дважды** — в миграции и константами Go — и связано
> тестом в обе стороны: восьмое значение в схеме валит его поимённо, опечатка в
> константе тоже. Иначе факт, записанный дважды, чинится один раз, чему эта фича
> научилась четырежды.
>
> **Предикат политики выписан целиком, а не опирается на родителя** — по правилу,
> которое запись шага 2 оставила этому шагу: делегировать «чьё» таблице-родителю
> можно ровно тогда, когда своей колонки владельца нет. Здесь она есть и вдобавок
> составляет половину первичного ключа.
>
> **`PRIMARY KEY (patient_id, entry_date)`, а не суррогат с уникальным индексом
> рядом.** §03 говорит «одна запись в день», и ключ, который это говорит, — это
> ключ, который умеет назвать `ON CONFLICT` шага 8. С суррогатным `id`
> конфликтовать было бы не по чему.
>
> **`source` не сужен грантом, и выразить это грантом нельзя.** Правило —
> «ставится один раз, не переписывается»; обработчик и вызывающий приходят к
> таблице **одной и той же ролью**, поэтому база не отличает, кто из них записал
> значение. Правило держит слияние, в Go, и оно там протестировано в обе стороны.
> Что пациент получает, записав колонку напрямую, — возможность пометить
> собственный день не тем словом: **в собственной ленте и в ленте своего врача**,
> потому что врач читает ту же строку и по ней же видит, родился день из мастера
> дозы или из ручной записи. Строка при этом остаётся его собственной, чужого дня
> он не касается. **Названная слабость, а не недосмотр.**
>
> **Правило «день должен что-то говорить» держат и Go, и схема, и путь дозы из него
> исключён.** Дублирование намеренное: по запросному пути отказ различает две
> причины и сообщает их листу, а на сервисном пути ограничение — единственное, что
> стоит между сидом и днём, которого пациент не заполнял.
>
> Исключение — не послабление, а перенесённое поведение, и сначала его здесь не
> было. `CadenceMocks.submit` пишет `event.asCheckIn(date)` безусловно, а
> `JournalWriteTest.anInjectionWithNoContextStillWritesItsDay` пинит результат
> словами «инъекция — факт сама по себе, поэтому она пишет день, у которого все поля
> пусты». Шаг 4 мастера доз — «всё по желанию», шаг 8 пишет событие и день **одной
> транзакцией**: без исключения пропущенный чек-ин валился бы с 23514 и откатывал
> запись дозы, а пропуск записи дневника вместо этого снимал бы с дня отметку «с
> дозой», которую показывает мобильный экран. Сервер и клиент разошлись бы о ленте.
> `source = 'dose'` — первый дизъюнкт ограничения, и он пришит тестом с обеих
> сторон: пустой день с `'dose'` принимается, с `'manual'` — нет.
>
> **`patient_id` удержан у `UPDATE` и оставлен у `INSERT`.** Это не помощь
> политике — она справляется, — а следствие того, что колонка составляет половину
> первичного ключа: перенос дня между пациентами это другая строка, а не правка
> этой. У `INSERT` решает `WITH CHECK`.
>
> **Общее ядро вынесено, не отложено.** `protocol` становился им по недосмотру:
> `Date`, `Slot`, `Range` и `UserID` объявились там на шаге 1, потому что первым
> понадобились там, и `inventory` с `journal` пришли импортировать контекст
> протокола ради одних только этих типов. Порог сначала записали на будущее —
> «если четвёртый контекст», — и тут же решили платить сразу.
>
> Типы переехали в `internal/platform/civil`. `api.md` кладёт общую проводку под
> `platform`, и это проводка: календарь не принадлежит ни одному контексту.
>
> Имя у́же содержимого сознательно. `Date`, `Slot` и `Range` — это гражданское
> время в обычном смысле, тот самый концепт, для которого у стандартной библиотеки
> Go типа нет. `UserID` — нет, и лежит там потому, что идентификатор пациента есть
> у всех четырёх клинических контекстов, а `identity` своего типа не экспортирует.
> **Если когда-нибудь начнёт — это первое, что оттуда уезжает.**
>
> Что вынос показал сразу: **`journal` перестал зависеть от `protocol` вовсе** —
> вся связь была этими типами. У `inventory` она настоящая (недельная ставка
> позиции), и осталась.
>
> Сторож на новый пакет утверждает, что он не импортирует **ничего** нашего и что
> его единственная зависимость — `time`: словарь, тянущийся обратно в контекст, был
> бы словарём того контекста под другим путём импорта.
>
> **Что закрыл второй раунд ревью.** `Merge` дедуплицирует теги **на обоих
> путях**: котлиновский `distinct()` идёт по всей склейке, поэтому первый чек-ин с
> `["nausea","nausea"]` сохранял дубль навсегда — каждое следующее слияние находило
> тег уже на месте. И `Merge` теперь возвращает ошибку, а не только строку: день
> другого пациента и день другой даты он **отказывается** сливать. Это
> единственное место в диффе, где кросс-тенантное **чтение** отмывалось бы в
> постоянную кросс-тенантную **запись** — строка ложилась бы законно своя, политики
> бы её приняли, и снизу никто бы не отличил. Сервисный шов читает эту таблицу с
> `USING (true)`, так что репозиторий, достающий день по одной дате, ровно такой
> `existing` и вернул бы.
>
> Тем же раундом закрыты шесть правил формы строки, которые не мерил ни один тест —
> включая сам `PRIMARY KEY`, заголовочное решение шага: их можно было удалить из
> миграции, и весь гейт остался бы зелёным. Каждое теперь припинено **поимённо** (не
> только по SQLSTATE: два ограничения на одной колонке отказывают одинаково, а в
> этой схеме одно уже пряталось за другим) и в обе стороны — отказ и приём на самых
> границах — по обе стороны и у отказа тоже: сначала каждая шкала пинилась с одного
> конца, и четыре расширения оставались живы (настроение до 9, энергия от 0, сон от
> 0 и до 8). Расширение — это ровно то направление, ради которого ограничения и
> стоят. Множество `source` сверяется со схемой так же, как теги. И пять
> контролей ролей, которых у этой таблицы не было ни одного: переназначение
> крест-накрест (видимость появляется у нового врача и **исчезает** у прежнего),
> админ, пишущий день пациенту, к которому его никто не приписывал, — вызывающий
> без личности (включая обнуление своих же claims внутри собственной транзакции) и
> сервисный путь, который читает и вставляет, но не переписывает и не удаляет. День
> редактируют, а не удаляют: по этой истории считается флаг побочки.
>
> Админский случай сначала был назван «`WITH CHECK` половины `FOR ALL`, до которой
> доходит только `INSERT`». Замерено: это не так — политика **без** `WITH CHECK`
> откатывается к `USING`, и удаление `WITH CHECK (true)` оставляет тест зелёным.
> Контроль настоящий, названная мутация — нет; ловит её только реестр предикатов.
> Название мутации, против которой тест не падает, — ровно тот дефект, о котором
> предупреждает эта же запись двумя абзацами ниже.

> **Что показала мутация.** Пятнадцать мутаций слияния и десять правил формы строки
> убиты поимённо. Одиннадцатое правило — «плоский, а не вложенный» у тегов —
> мутацией не проверялось и **тестом не держалось вовсе**: каждая строка набора
> плоская или пустая, а на плоском входе `CASE` тождественен голому
> `tags <@ ARRAY[…]`. Замерено, что `ARRAY[['nausea']]` в одномерное множество
> **входит**, то есть без этой ветки вложенный массив легальных имён лёг бы в
> таблицу и потом никогда не разобрался бы в `[]Tag`. Ложное «убито» читается
> ровно как убито — и это второй раз за шаг, когда мутация умирала не от того теста,
> который её называет. Два предиката политики выжили в пакете `journal` и оба **закрыты
> вторым механизмом**, а не тестом:
>
> - `WITH CHECK` пациентского `UPDATE` **не может провалиться** при нынешних грантах —
>   `patient_id` у `UPDATE` удержан, поэтому отдельная передача дня отказывается как
>   **право**. «Недостижим» было бы неверно, и так тут сначала и стояло: на апсерте
>   шага 8 грант для колонки, которой нет в `SET`, не спрашивают вовсе, и отвечает
>   именно эта политика — по неизменному `patient_id`, то есть обе половины это одно
>   и то же выражение;
> - второй конъюнкт врачебного `EXISTS` избыточен, пока стоит
>   `care_team_assignments_mine_provider`: подзапрос идёт от имени вызывающего, и RLS
>   на таблице назначений уже сужает его до строк этого врача.
>
> Обе — глубина обороны, и держит их **реестр предикатов**, а не поведение: убрать
> любую из них валит `TestThePolicyPredicatesAreTheOnesDeclared` и ничего больше.
> Записано здесь и однострочником у каждой политики, потому что обе выглядят
> избыточными ровно настолько, чтобы следующий читатель их «починил».

> **Апсерт кросс-тенантно недостижим по построению, и это сильнее, чем «отказан».**
> `patient_id` — половина арбитра, а `WITH CHECK` у `INSERT` применяется к каждой
> предложенной строке **до** того, как ищется конфликт, поэтому до ветки
> `DO UPDATE` дело не доходит. Контроль сначала был написан так, будто он эту ветку
> меряет, и утверждал регрессию, которой не держит: суррогатный `id` с той же
> уникальностью оставляет его зелёным. Теперь проверяется **сообщение**, а не только
> `42501` — отказ со стороны `INSERT` говорит «new row violates», отказ по пути
> конфликта называет `USING`, и только это их различает. **Свойство принадлежит
> арбитру, а не ключу: если шаг 8 станет апсертить по другому арбитру, контроль
> надо переписывать тогда же — здесь этого не заметит ничто.**

> **К шагу 8** (слияние чек-ина): оба сторожа слияния — программистские ошибки по
> собственному описанию, и обработчик обязан отдавать по ним **500**, а не 4xx:
> `errors.Is` приглашает сделать наоборот, а там текст уезжает вызывающему. Сами
> сообщения переписаны так, чтобы называть правило, а не факт о чужой строке —
> иначе они восстанавливают ровно тот оракул («писал ли тот дневник в этот день»),
> который на стороне базы закрыт пробой на существующий ключ. И `patient` в `Merge`
> — субъект токена, не поле запроса; вся кросс-тенантная защита стоит на этом.

> **К шагу 8** (слияние чек-ина): `ParseTag` и `ParseSource` — конструкторы с
> проверкой для обоих закрытых множеств; на границе транспорта строка становится
> типом там, а не здесь. И три правила журнала, которые держит Go и которых схема
> не выражает: провенанс ставится один раз, пустая заметка — пропущенная, теги
> накапливаются без дублей.

> **К финализации:** `api.md` перечисляет `internal/platform/{auth, config,
> database, httpserver, testsupport}` — список пополняется `civil`.

### step-5: Схема дозирования
`dose_events` парой миграций: полная форма §03 включая десять зон, идемпотентность по
`(patient_id, client_request_id)` и частичный уникальный индекс на слот.
todoist: "6hH8gjcWjGWmMFMH"

> [!deviation] 2026-08-22 — шаг 5
> **Цепочка составных внешних ключей вместо предиката.** Событие дозы несёт три
> ссылки, и каждая кому-то принадлежит. Событие, указывающее на чужой курс, чужую
> позицию или чужой флакон, было бы строкой законно своей: политики её примут, врач
> прочтёт как историю своего пациента, а списан будет чужой флакон. Это форма, с
> которой шаг 2 встретился как с перепривязкой. `CHECK` подзапроса не несёт, а
> политика стережёт только запросный путь, поэтому владение связано **ключами**:
>
> ```
> (patient_id, protocol_id)       → protocols (patient_id, id)
> (protocol_id, protocol_item_id) → protocol_items (protocol_id, id)
> (patient_id, vial_id)           → vials (patient_id, id)
> ```
>
> Три родителя получили составные уникальные свидетели под это. Отказ измерен и на
> **сервисном пути** — ради чего всё и делалось: там политику не спрашивают вообще.
>
> **Отсюда `protocol_id` колонкой.** Он выводится из позиции, то есть выглядит как
> запрещённое здесь хранение производного, — и не является им: это половина ключа,
> и ровно та половина, которая делает цепочку выразимой.
>
> **`compound_id` снимается на событие — решение, оставленное шагу 5.** Шаг 2 записал:
> «колонка становится решаемой ровно тогда, когда появляется
> `dose_events.protocol_item_id`». §03 даёт этой таблице свои `dose_value` и
> `dose_unit` именно для того, чтобы число пережило правку курса; атрибуция —
> факт клинически весомее числа, и она ехала на изменяемом
> `protocol_items.compound_id`. Теперь снимается при записи, а колонка позиции
> остаётся редактируемой: врач, ошибшийся препаратом, чинит позицию, а не теряет
> курс. Пришито тестом: перепрописал позицию — событие по-прежнему называет тот
> препарат, который пациент себе ввёл.
>
> **Десять зон прочитаны, а не выведены.** §03 называет три и пишет «(10 zones)».
> Остальные — карта тела замороженного прототипа (`ZONES_FRONT` шесть спереди,
> `ZONES_BACK` четыре сзади). Сверено с обоими источниками; двух выдуманных
> («l-flank», «r-flank»), про которые говорит KDoc в KMP, в прототипе нет.
>
> **Семь побочек теперь записаны трижды** — здесь, в `000017` и константами Go. Это
> хуже, чем дважды, и `CHECK` не умеет ссылаться на чужое ограничение. Связано
> тестом, который читает **оба** определения из каталога и сверяет с Go. Что закрыло
> бы это по-настоящему: одна `IMMUTABLE`-функция в `app`, на которую смотрят оба
> `CHECK`; это правка уже применённой миграции шага 4, то есть расширение объёма —
> **названо, не сделано молча.**
>
> **`ON DELETE RESTRICT` на всех трёх, и позиционный ключ — тот, ради которого это
> важно.** Сначала у курса и позиции стояло `CASCADE`, и это было **найдено и
> замерено ревью как критическое**: `cadence_service` держит `DELETE` на
> `protocol_items` с `USING (true)` — ровно то, чем редактор курса шага 6 убирает
> строку из назначения, — а ссылочное действие исполняется от имени владельца
> таблицы и не спрашивает ни грант, ни политику. То есть врач, убравший позицию,
> стирал все дозы, которые пациент по ней записал, молча возвращая их флакону, чей
> остаток есть вычитание по этим самым строкам. Это противоречило заголовку
> `000020` и вот этой самой записи. Ссылка на флакон уже стояла `RESTRICT` по той же
> причине — две ссылки спорили друг с другом, и не спорил с ними никто, потому что
> **ссылочное действие не проверял ни один тест** — ни здесь, ни в предыдущих
> таблицах. Теперь проверяет, и все **четыре** ссылки поимённо: позиция с записанной дозой,
> курс с записанной дозой, флакон, из которого дозу набрали. Третья добавилась
> вторым раундом — сначала запись говорила «в обе стороны», перечисляя две; у
> флакона `DELETE` есть только у админа, и в наборе аптечки доз нет вовсе, так что
> `CASCADE` на нём пережил бы всё. Четвёртая, на справочник препаратов, добавилась
> **третьим** раундом и оказалась ровно в том же состоянии колонкой левее: она в
> другой категории — стережёт атрибуцию, а не арендатора, — и потому выпадала из
> перечисления «на всех трёх» дважды подряд. Ключ теперь назван
> (`dose_events_attributed_to_a_compound`), а не оставлен под именем, которое
> придумает PostgreSQL. Один и тот же промах трижды на четырёх ссылках: **список,
> который перечисляет, надо сверять с таблицей, а не с предыдущим списком.** А удаление самого пациента по-прежнему уносит его
> курс и его дозы — это замерено, а не выведено, потому что порядок срабатывания
> ссылочных триггеров у одной таблицы обсуждению не подлежит.
>
> **Индекс слота ведёт `patient_id`, и это не украшение.** Проверка уникальности
> обходит RLS по устройству и достигается при вставке кортежа — раньше ссылочных
> проверок, которые суть `AFTER ROW`-триггеры. Замерено на исходной форме индекса:
> пациент, назвавший чужую позицию, получал `23505` там, где тот пациент дозу
> записал, и `23503` там, где нет. Бит чужой истории инъекций на пробу, с перебором
> по датам — форма, которую дневник встретил шагом раньше через свой первичный ключ.
> Предусловие — владеть чужим `protocol_item_id`, а его RLS не выдаёт; это то, что
> держало масштаб, а не то, что закрывало дыру. Закрыто тем, что в ключ добавлен сам
> вызывающий: проба падает в собственное пространство ключей и столкнуться не может,
> то есть порядок перестаёт иметь значение вместо того, чтобы на него опираться.
> Уникальность при этом та же — позиция принадлежит ровно одному пациенту по цепочке.
>
> **Ни `UPDATE`, ни `DELETE` никому, кроме админа.** Записанная доза — факт: остаток
> флакона это вычитание по этим строкам, приверженность считается по ним же, а
> прототипный тумблер «отменить запись» заменён экраном деталей (план сборки, §08).
> Ошибка правится клиникой, а не исчезновением строки. Поэтому у пациента нет
> колоночного списка `UPDATE` — у него нет самого гранта.
>
> **Идемпотентность ограничена пациентом, а не глобальна.** `UNIQUE (patient_id,
> client_request_id)`: ключ генерирует клиент, и два устройства двух разных
> пациентов, выдавшие одинаковый ключ, не должны сталкиваться — иначе один из них
> молча теряет дозу. Замерено в обе стороны. Отдельно частичный уникальный индекс на
> слот ловит гонку, которой ключ не видит: два разных ключа в один слот.
>
> А вот **частичность индекса поведения не меняет**, и сначала здесь было написано
> обратное. Замерено на postgres:17-alpine — том образе, который проект пинит, а не
> на 16, как было записано сначала: тотальный уникальный индекс принимает
> два NULL-слота и отказывает двум названным — NULL в уникальном индексе различны,
> и бесслотовый день держит именно это, а не `WHERE`. Мутация, снимающая `WHERE`,
> выжила — и правильно сделала. Оговорка остаётся ради размера индекса: строки,
> которые он ограничить не может, в дерево не попадают.
>
> **К шагу 8, про `compound_id`:** его ставит сервер из разрешённой позиции и
> **никогда** из тела запроса. Колонка входит в пациентский грант `INSERT`, и
> связать её ключом нельзя, не убив сам смысл снимка, — значит единственный сторож
> живёт на пути записи. Тест шага 5 показывает лишь, что отдельная колонка не едет
> при правке чужой таблицы; он прошёл бы точно так же, если бы клиент назвал
> препарат, которого позиция не прописывала.
>
> **Запись дозы не пишет строку аудита, а `dosing.md` обещает обратное.** Контракт
> компонента говорит про `POST /me/dose-events` «…and writes the audit record»;
> гранты, решённые здесь, это закрывают: доза — это пациент про себя, то есть
> `WithCaller`, RLS и без аудита, и у `cadence_patient` нет и не будет права писать
> в `audit_log`. Так же устроены дневник и аптечка, но у них в заметках такого
> обещания нет — то есть расхождение заводится **здесь**. Названо на шаге, который
> его создал, а не оставлено шагу 8 как выбор между нарушением записанного контракта
> и второй транзакцией под сервисной ролью, которую никто не закладывал.
> **К финализации:** строку про аудит в `dosing.md` снять или переписать под
> сервисный путь.
>
> **Ограничение фото названо на уровне таблицы.** Встроенный `CHECK`, читающий вторую
> колонку, всё равно становится табличным — под именем, которое придумывает
> PostgreSQL (`dose_events_check`), с которым столкнётся следующее такое же и которое
> читателю не говорит ничего. Форма ключа — из шага 3: не префикс, а сегменты.

### step-6: Справочник и CRUD протокола
Эндпоинты создания и правки протокола с вложенными позициями и фазами, только своим
пациентам, с аудитом. Разрешение препарата: по `compoundId` либо заведение по имени с
нормализацией. Проверка покрытия фаз в Go до вставки, чтобы отказ был внятным.
todoist: "6hH8gjh8695jJRgq"

> [!deviation] 2026-08-22 — шаг 6
> **Три долга, оставленные шагами 1 и 2, закрыты здесь, и шов у них один:** строка
> впервые становится значением закрытого множества именно в этом CRUD.
> `ParseStatus`/`ParseCadence`/`ParseKind`/`ParseDoseUnit` в форме `(T, bool)`, каждый
> читает **единственное** объявление своего набора, а не собственный литеральный
> список — парсер со своей копией это вторая копия. `civil.Date.Valid()` через
> круговой обход, а не по таблице длин месяцев: високосный февраль получается сам.
>
> **`Draft.Check` держит девятнадцать правил, и это два разных списка, а не один.**
> Схема отказывает именем ограничения и без строки — до врача это доходит как «23514»
> поверх формы из двенадцати позиций. Go говорит, какая позиция и какое поле, вовремя
> для 422.
>
> **Четырнадцать правил дублируют схему**, и тест предъявляет их семнадцатью случаями —
> у трёх правил по две стороны. Каждый такой отказ схема обязана повторить: тем же
> поимённо названным ограничением, а для невозможной даты своим кодом `22008`, потому
> что её схема отвергает как значение, а не по правилу.
>
> **Пять правил схема не выражает вовсе**, плюс верхняя половина шестого: пустой курс,
> позиция без фаз, препарат у инъекции, препарат у не-инъекции, дважды названная позиция
> (это две строки запроса, а схема видит по одной) — и `to_week` против
> `duration_weeks`, которые лежат в разных таблицах. Их держит только Go.
>
> **Разбиение оказалось неверным трижды подряд, и каждый раз одинаково.** Сначала «все
> восемнадцать дублируют схему» при девяти случаях сличения из шестнадцати. Потом «все
> четырнадцать» — а `ErrDrugNotDescribed`, добавленное тем же раундом, и `ErrNotADay` не
> попали ни в один из двух списков. Потом «семнадцать», где семнадцать было числом
> **строк таблицы**, а не правил, и две половины не сходились в сумме. Каждый раз
> множества были верны, а число — нет.
>
> **Число про множество надо считать по множеству, а не по предыдущему числу** — тот же
> промах, что трижды вышел с четырьмя ссылками на шаге 5, и он переживает зелёный гейт,
> потому что арифметику в прозе не проверяет ничто.
>
> **Разрешение препарата — `ON CONFLICT DO NOTHING` плюс `SELECT`**, как записал шаг 2:
> идиома `DO UPDATE … RETURNING` требует `UPDATE`, которого у сервисного пути на
> справочнике нет намеренно — столкновение имён не должно переписывать строку, на
> которую уже ссылаются чужие протоколы. Цена — лишний круг до базы, и она записана
> у самой функции, чтобы следующий читатель это не «починил». Регистр держит индекс
> по `lower(name_ru)`, пробелы — Go: `lower()` оставляет « Семаглутид » отличным от
> хранимого, и вышло бы две строки.
>
> **Ссылка на препарат уехала внутрь позиции.** Сначала она шла отдельным срезом
> рядом с `Items` — это ровно та параллельная пара, где четвёртая позиция получает
> препарат третьей. Заодно стало выразимо второе правило: у не-инъекции препарата
> быть не должно, потому что взвешивание с препаратом — это форма, заполненная не в
> той строке.
>
> **`Replace` правит по идентификатору позиции, а не удаляет всё разом.** Названную
> позицию переписывают на месте, неназванную заводят, отсутствующую удаляют — и вот
> там `000019` держит `RESTRICT`: позицию, по которой записана доза, удалить нельзя, и
> врачу отвечают, что остановить её надо снятием `loggable`. История клиники не
> стирается правкой назначения.
>
> Сначала здесь было написано обратное — «у позиции нет идентичности, которую клиент
> держит между правками» — и это была посылка, из-за которой всё ломалось; подробности
> ниже, в разделе про два критических. **Посылка снята, и это важно шагу 8:**
> идентификатор позиции теперь приходит **от клиента** в таблицу, у которой нет своего
> `patient_id` — она наследует арендатора через `protocol_id`. Поэтому каждый оператор,
> берущий `protocol_item_id` из запроса, скоупится по курсу **в самом операторе**, а не
> одной проверкой в Go: `UPDATE` по паре, удаление фаз через `USING`, вставка фаз через
> `SELECT`. Свидетель `RowsAffected` — у `UPDATE` и у вставки фаз; у удаления фаз его нет
> и быть не должно, ноль удалённых фаз там законный исход, а свидетель ему — прошедший
> перед ним `UPDATE`.
>
> Что это **второй замок, а не украшение, — замерено**: со снятым Go-сторожем скоуп
> отказывает сам; со снятыми обоими фазы каллера ложатся на позицию другого пациента, и
> кросс-курсовый тест читает 1,5 мг там, где тому прописали 0,25 и 0,5.
>
> **И один разрушительный дефект той же природы, найденный третьим раундом:** зачистка
> неназванных позиций сравнивала `id::text` — всегда каноничный нижний регистр от
> `uuid_out` — с написанием, которое прислал каллер. И `IsUUIDShaped`, и `format:"uuid"`
> у huma принимают заглавный. Позицию, названную как `5D4F3B7C-…`, оператор выше
> обновлял на месте (он сравнивает uuid'ы), а этот — удалял, вместе с фазами. Врач
> просил её **оставить**. А если на неё ссылалась доза, он получал 409 с советом снять
> `loggable` у позиции, которую не собирался убирать. Сравнение теперь
> `id = ANY($2::uuid[])`, и есть тест на заглавное написание.
>
> **Справочник заполняется одним проходом в фиксированном порядке**, до записи позиций.
> `app.compounds` — единственная общая таблица на этом пути, и два курса для **разных**
> пациентов брали на ней индексные блокировки в порядке своих запросов: {X, Y} и {Y, X}
> одновременно — взаимная блокировка, и запись одного пациента падает из-за другого.
> Сортировка по нормализованному имени снимает цикл.
>
> **Зачистка проверялась на базе с одним курсом**, где `protocol_id = $1` неотличим от
> его отсутствия — а без него правка удаляет **все** позиции в базе, которых запрос не
> назвал. Теперь у второго пациента есть свой курс, и его строки пересчитываются после
> чужой правки.
>
> `UPDATE` самого курса тоже скоупится по пациенту, но это **единообразие, а не второй
> замок**, и так и записано у кода: замерено — снятие скоупа не меняет поведения ни
> одного теста и не могло бы, потому что со снятым сравнением владельца он обновил бы
> ноль строк, а не отказал. Замок здесь — `ownerOf`.
>
> **Авторизация целиком в Go, и это здесь единственная защита.** Запись
> кросс-акторная: `cadence_doctor` не имеет `INSERT` ни на одной из этих таблиц,
> значит сервисный шов — а там все предикаты `USING (true)`. Проверка команды
> ухода поэтому своя, со своим тестом крест-накрест на двух врачах и двух пациентах,
> плюс админ, сам пациент и запрос вообще без принципала.
>
> **Неназначенный врач и не-врач получают один и тот же 403.** Сказать первому, что
> пациент существует, — это факт о чужой команде ухода.
>
> И это было **написано раньше, чем стало правдой**. `Replace` читал владельца курса
> до авторизации, поэтому неназначенный врач, держащий идентификатор курса, получал
> 403, когда курс существовал и принадлежал названному пациенту, и 404 в остальных
> случаях: бит `protocols.patient_id` на запрос, тому, кому RLS не показала бы ничего.
> Обычный путь к такому — врач, у которого строку назначения удалили, потому что
> удаление пары и есть весь механизм отзыва. Порядок теперь обратный: сначала
> авторизация по пациенту из пути, потом чтение владельца, — и в отказе владелец не
> называется вовсе. Пришито тестом: настоящий курс и случайный uuid дают **один и тот
> же** ответ. Так же устроена запись в `identity`, и это была форма, которую шаг
> должен был повторить.
>
> **Форма идентификатора проверяется до открытия шва.** Неудачный каст uuid внутри
> транзакции — это 500 там, где место отказу. Живёт в `write.go`, а не в
> `Draft.Check`: сторож чистоты не пускает пакет базы в генератор.
>
> **Два критических, оба найдены ревью и оба измерены.**
>
> **Эндпоинты не принимали ни одного запроса.** `times` объявлено `format:"time"`,
> huma читает это как RFC 3339 full-time, а обработчик разбирал `15:04`. Замерено на
> huma v2.39.0: «08:00» отвергается валидацией, «08:00:00» проходит её и падает на
> разборе. Ни одно значение не проходило обе проверки — весь шаг был недоступен через
> транспорт. Не видел этого никто, потому что **ни один тест не гнал запрос через
> зарегистрированный хендлер**: набор звал `Course.draft()` напрямую, минуя слой, где
> живёт сгенерированная схема. Теперь `times` — это `pattern` под `HH:MM`, как в §03,
> в `LocalTime` KMP и в прототипе, и есть тест, который ведёт запрос через chi и
> `httpserver.NewAPI`, как это делает `identity`.
>
> **Курс переставал редактироваться после первой записанной дозы.** `Replace` удалял
> все позиции безусловно, а `dose_events` держит позицию `RESTRICT`. То есть отказ
> получала любая правка — включая смену статуса, то есть **отмену и завершение
> курса**, и включая снятие `loggable`, то есть ровно то лекарство, которое называл
> ответ 409. Тупик без выхода, и титрация по ходу курса — основной клинический цикл
> продукта — была невозможна. Имя теста при этом говорило «keeps», а не keeps'илось
> ничего.
>
> Позиция теперь несёт свой идентификатор на проводе: `Create` отвечает ими по
> порядку именно для этого, редактор возвращает те, что оставил. Названную позицию
> переписывают на месте, неназванную заводят, отсутствующую удаляют — и вот там
> `RESTRICT` и говорит. `UPDATE` при этом скоупится **и по курсу**, а не только по
> строке: сервисный шов не несёт предиката, и один идентификатор позволил бы врачу
> переписать позицию чужого курса. Свидетель — `RowsAffected`, потому что
> отфильтрованный `UPDATE` возвращает успех и ноль.
>
> **Ещё один major из того же раунда:** три `NOT NULL`-колонки нового препарата не
> спрашивал никто, и запрос, законный по опубликованному контракту, доходил до
> `INSERT`, ловил 23514 и возвращался **500** — ровно тот исход, ради отказа от
> которого написан весь `shape.go`.
>
> И `TestTheSetsAreTheOnesTheSchemaNames` (теперь `TestTheSetsTheSchemaNamesAreTheOnesGoDeclares`) схему не читал: он сравнивал Go с литералом,
> написанным рядом. Пятый статус, добавленный будущей миграцией, оставил бы гейт
> зелёным, а API молча отказывал бы в законном назначении. Теперь читает
> `pg_get_constraintdef`, как это делает шаг 4 с тегами.
>
> **Мутации, двумя прогонами.** Девятнадцать по коду шага — все убиты поимённо. И
> отдельный прогон по починкам первого раунда: правка позиции на месте, скоуп по курсу,
> `RowsAffected`, три поля препарата, аудит правки — тоже все убиты. Две пришлось
> перемерить: первая попытка ломала **число параметров**, а не правило, и ошибка сборки
> запроса читается ровно как убитый мутант. Один выживший оставлен и объяснён: снятие
> `pattern` у `times` поведения не меняет (разбор отказывает тем же 422 слоем ниже),
> держит его проверка дрейфа контракта — это обещание сгенерированному клиенту, а не
> второй сторож.
>
> И одно ложное измерение из первого прогона: тест на `Valid()` лежал в
> пакете-**потребителе**, и сам `civil`, прогнанный отдельно, принимал табличную
> замену, допускающую 30 февраля. Тест переехал туда, где живёт функция.
>
> **Что поймал сам тест:** первая версия набора утверждала, что «Сема глутид»
> (с неразрывным пробелом) — то же лекарство, что «Семаглутид». Это не так: слово,
> разорванное пробелом, — другое имя, и нормализатор прав. Схлопывание пробелов
> может показать только многословное имя, и теперь показывает.

### step-7: Подсказка места инъекции
Порт `InjectionRotation.kt` в `dosing`: следующее место из истории. Нужен агрегату
«Сегодня», поэтому идёт до него.
todoist: "6hH8gjjcXpg925wH"

> [!deviation] 2026-08-23 — шаг 7
> **Порядок множества зон оказался нагруженным, и он не прототипный.** Тай-брейк
> ротации — это порядок объявления енума в KMP, и от него зависит, что предлагают
> пациенту **без истории**. Шаг 5 положил `Sites()` в порядке отрисовки прототипа
> (шесть спереди, четыре сзади) с комментарием «порядок прототипа» — это порядок
> **панелей** клиента, вопрос отрисовки. Прочитанный сюда, он сдвинул бы стартовую
> подсказку с левого живота на правое плечо, против того, что документирует
> `log-dose/data.ts`. Порядок исправлен, и комментарий теперь говорит, что он
> нагруженный, а не косметический.
>
> **Вход — `Injection{Site, At}`, а не событие дозы**, как `protocol` берёт
> `LoggedSlot`: функция чистая, и десяток полей ей читать незачем. Это заодно
> **усиливает** один из перенесённых сторожей: KMP замораживает `scheduledFor` и
> `createdAt`, чтобы ротация, потянувшаяся к соседнему полю, провалилась, — здесь
> этих полей просто нет, и прочитать то, чего не дали, нельзя.
>
> **Один перенесённый тест снят, а не перенесён.** `aBackDatedLogCountsFromWhenItWas
> InjectedNotWhatItWasScheduledFor` в KMP различал по `scheduledFor`; здесь этого поля
> нет, поэтому копия побайтово совпала с соседкой и упасть по названной причине не
> могла — ни одна мутация не убивала её в одиночку. Сторож стал структурным, и его
> **измеримая часть переезжает** на шаг, где появится чтение `dose_events` и маппинг
> строки в `[]Injection`: различать `injected_at` от `scheduled_for_date` и `created_at`
> будет там. **К шагу 8.**
>
> **Порядок пришит целиком, литералом.** Сначала были закреплены только первые две
> позиции — измерено ревью: перестановка остальных восьми оставляла весь пакет зелёным.
> Сторож фикстуры при этом сортирует обе стороны, то есть стирает ровно то свойство,
> которое шаг объявил нагруженным. Добавлен и второй тест, который доводит несвежесть
> по всем десяти позициям, а не по одной.
>
> **Два теста сверх переноса, оба про Go.** Нулевое `time.Time` — настоящий момент
> времени, поэтому «записи нет» и «использована в нулевой момент» обязаны остаться
> разными ответами. И арифметика самой фикстуры: набор, у которого даты шли бы в другую
> сторону, заставил бы каждый тест выше утверждать своё зеркальное отражение и пройти.
>
> Семь мутаций логики самой функции убиты поимённо — **и утверждение «все убиты» было
> неверным**: мутация порядка множества в их число войти не могла, потому что теста на
> него не было. `git diff main -- kmp/` пуст.

### step-8: Запись дозы
`POST /v1/me/dose-events` одной транзакцией: идемпотентность, разрешение слота,
разрешение флакона, событие, запись дневника, ответ закрытым множеством исходов.
todoist: "6hH8gjq7jW7RhH7H"

### step-9: Агрегаты «Сегодня» и расписания
`GET /v1/me/today`, `GET /v1/me/schedule?month`, `GET /v1/me/schedule/day?date` в форме,
которую ждут `TodaySummary` и `ScheduleDay`. Поля питания и измерений отдают `null`
явно — их контексты не построены.
todoist: "6hH8gjrPw3q85pJq"

### step-10: Хранилище и подписанные ссылки
Пакет `platform/storage` за интерфейсом, объявленным потребителем, с тестом
направления импорта. Конфигурация без запасных значений. Выдача коротких подписанных
ссылок после проверки видимости строки.
todoist: "6hH8gjwXjFXRJGcH"

### step-11: Сид протокола
Расширение `cmd/seed` полным многокомпонентным протоколом: двенадцать недель
титрованного семаглутида, BPC-157 дважды в день, вечерняя добавка и воскресное
взвешивание — чтобы все категории вхождений были видны, а не только инъекции.
todoist: "6hH8gjvCH7w8MjxH"

### step-12: Контракт и реестр расхождений
`make openapi` перегенерирован и закоммичен, гейт дрейфа зелёный. Расхождения этого
блока с планом партнёра и с прототипом записаны в реестр.
todoist: "6hH8gm29pc5gwWvH"

> [!deviation] 2026-08-15
> Предложение, пункт 2, втягивает **редактор протокола** в этот блок. Спека выносит его
> в отдельную. Почему: при написании спеки выяснилось то, чего при одобрении
> предложения видно не было — редактор стоит на «Скелете дашборда» и «Первом живом
> чтении», а `web/` не существует вовсе, там только замороженный прототип. Обе спеки в
> `approved`, ни одна не построена. Держать редактор здесь значило бы иметь спеку,
> которую нельзя довести до конца своими силами.
>
> Из первой передачи редактор **не выпадает** — он идёт своей спекой сразу после тех
> двух, и порядок фаз это уже отражает. Меняется граница спеки, не объём передачи.

## Открытые вопросы

> [!decision] 2026-08-15 — **у пациента один активный протокол.** Закрепляется
> частичным уникальным индексом `(patient_id) WHERE status = 'active'` и записывается
> инвариантом компонента, а не только индексом: клиент везде берёт ровно один
> `ProtocolPlan`, и без этого правила его форма была бы просто неверной.
>
> «Пока» здесь настоящее. Снять ограничение позже — это миграция (индекс уходит),
> правка формы клиента (`ProtocolPlan` становится списком) и решение о том, что
> означает «доза на сегодня», когда протоколов два. То есть отменять его дороже, чем
> вводить, и делать это стоит, только если клиника принесёт настоящий случай двух
> одновременных курсов.

> [!decision] 2026-08-15 — **докупка молчит для препаратов без ставки.** Редактор
> лишнего поля не просит. Ставка из миграции нужна ровно в одном случае — когда
> активный протокол препарат не покрывает; пока он покрывает, аптечка считает по
> текущей фазе титрации, что точнее. Для заведённого врачом препарата подсказка
> докупки просто не выводится: молчание честнее выдуманного числа, а врач, который
> завёл препарат, знает про него больше нашей константы.

> [!decision] 2026-08-15 — **повтор с изменившимся телом отвечает конфликтом.**
> Тот же `clientRequestId` с другим черновиком — это ошибка клиента, а не повтор.
> Молчаливый возврат первого результата спрятал бы её ровно в том пути, который
> существует ради ненадёжной сети: очередь повтора отправляет то, что сохранила, и
> расхождение означает, что она сохранила не то. Сравнивается смысловое содержание
> черновика, а не байты запроса, и отказ называет, какое поле разошлось.

> [!decision] 2026-08-15 — **`code` остаётся на сервере и уходит из доменного типа
> KMP.** Проверено: поле пишет только сид, не читает никто (совпадения `.code` в
> остальном коде — это `Metric.code`, другой тип). Но серверная роль у него есть, и в
> клиенте её не видно: справочник сеется **по коду**, поэтому повторный прогон
> миграции идемпотентен и переименование `name_ru` не заводит второй препарат.
> Поэтому на сервере колонка остаётся и становится **nullable** — у препарата,
> заведённого врачом, кода нет, — а из `Compound` в KMP поле убирается при
> регенерации типов в фазе 4: носить поле, которое существует только чтобы его
> игнорировали, незачем.

> [!decision] 2026-08-21 — **разрывы между фазами законны, и нота компонента
> приводится к этому, а не наоборот.** `protocol.md` инвариант 4 требовал, чтобы
> фазы покрывали длину протокола ровно: «no gaps, no overlaps, nothing past the
> boundary». Приёмка этой спеки требует обратного, и права она: `DoseBandsTest.kt`
> (`aGapBetweenTwoPhasesIsAGapBetweenTwoBands`, `:119-133`) держит `BROKEN_PHASES`
> с дырой на неделях 5–8 и проверяет, что над ними **ничего не рисуется**. Вымыв
> между фазами — это предписание, а не пропуск, и клиент так себя уже ведёт.
>
> Следствия, обязательные к исполнению:
> - шаг 2 — `EXCLUDE USING gist` сторожит **только перекрытие**; сплошность база
>   не проверяет;
> - шаг 6 — «проверка покрытия фаз» читается как проверка **непротиворечивости**:
>   Go отвергает перекрытие и выход за `duration_weeks`, разрыв пропускает;
> - при финализации инвариант 4 в `protocol.md` переписывается, а не дополняется —
>   иначе в ноте окажутся два инварианта, отрицающих друг друга.
