{
  "components": {
    "schemas": {},
    "securitySchemes": {
      "bearer": {
        "bearerFormat": "JWT",
        "description": "A Supabase access token. Verified against the project's JWKS on every request.",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "title": "Cadence API",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "security": [
    {
      "bearer": []
    }
  ]
}
